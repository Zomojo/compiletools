language: python
python:
    - "3.6"
    - "3.7"
    - "3.8"
    - "pypy3"
dist: bionic

# command to install dependencies      
install:
    - pip install -r requirements.txt 

# command to run tests
script:
    - nosetests
